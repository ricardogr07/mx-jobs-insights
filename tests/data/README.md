# Test Data

This tree stores checked-in upstream source fixtures for reproducible tests.

- `upstream_workspace/` mirrors the upstream workspace contract used by the
  source adapters.
- Tests must copy these fixtures into `tmp_path` before mutating them.

